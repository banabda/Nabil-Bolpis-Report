import 'package:flutter/material.dart';

var textInputDecoration = InputDecoration(
  border: InputBorder.none,
  hintStyle: TextStyle(color: Colors.grey[400]),
);

var boxInputDecoration = BoxDecoration(
  border: Border(
    bottom: BorderSide(
      color: Colors.grey[100],
      )
      )
);
